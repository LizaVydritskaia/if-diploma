export const API = 'https://if-modnikky-api.onrender.com/api';

export const API_catalog = `${API}/catalog`;

export const API_cart = `${API}/cart`;

export const API_subscription = `${API}/subscription`;
